#include <stdio.h>
#include <math.h>

//ex 3. Faça um programa que leia um número real x e calcule o valor de f(x) = px+ (x/2) +xx. (pesquise sobre as funções sqrt e pow).

int main()
{
   int x;

   printf("Digite um valor para x:\n");
   scanf("%i", &x);

   x = sqrt(x)+(x/2)+pow(x,x);

   printf("O resultado e: %i\n", x);


}
